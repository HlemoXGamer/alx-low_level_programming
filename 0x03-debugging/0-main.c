#include "main.h"

/**
 * main - prints the status of an integer
 * positive_or_negative - print int status
 * Return: Always 0
 */

int main(void)
{
	int i = 0;

	positive_or_negative(i);

	return (0);
}
