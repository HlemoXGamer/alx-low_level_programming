#include <stdio.h>

/**
 * main - prints the phrase with proper grammer,
 * but the outcome is a piece of art,
 * Returm: 0 if true, false otherwise
 */
int main(void)
{
	printf("%s\n","with proper grammar, but the outcome is a piece of art,");
	return (0);
}
